package org.openu.fimcmp.eclat;

import scala.Tuple2;

import java.util.List;

/**
 * The main class that implements the Eclat algorithm.
 */
public class EclatAlg {
    /**
     * Returns a list of {FI as an array of r1s, its frequency}
     * @param tidSets list of {rankK, bit array of TIDs}
     */
    List<Tuple2<int[], Integer>> computeFisFromTidSets(List<long[]> tidSets, int minFreq) {
        //TODO
        return null;
    }
}
